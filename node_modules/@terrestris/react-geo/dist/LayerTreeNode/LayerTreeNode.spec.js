"use strict";

var _LayerTreeNode = _interopRequireDefault(require("./LayerTreeNode"));

var _TestUtil = _interopRequireDefault(require("../Util/TestUtil"));

var _propTypes = _interopRequireDefault(require("prop-types"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

/*eslint-env jest*/
describe('<LayerTreeNode />', function () {
  var defaultProps = {
    inResolutionRange: true,
    filterTreeNode: function filterTreeNode(ftn) {
      return ftn;
    }
  };
  it('is defined', function () {
    expect(_LayerTreeNode["default"]).not.toBeUndefined();
  });
  it('can be rendered', function () {
    var wrapper = _TestUtil["default"].mountComponent(_LayerTreeNode["default"], defaultProps, {
      context: {
        rcTree: {
          prefixCls: '',
          registerTreeNode: function registerTreeNode() {}
        }
      },
      childContextTypes: {
        rcTree: _propTypes["default"].object
      }
    });

    expect(wrapper).not.toBeUndefined();
  });
});